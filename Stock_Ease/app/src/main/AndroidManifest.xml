<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StockEase"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.client.ClientDashboardActivity"/>
        <activity android:name=".ui.employee.EmployeeDashboardActivity"/>
        <activity android:name=".ui.owner.OwnerDashboardActivity"/>
        <activity android:name=".ui.login.LoginActivity"/>
        <activity android:name=".ui.register.RegisterActivity"/>
        <activity android:name=".ui.owner.CreateProductActivity"/>
        <activity android:name=".ui.owner.InventoryOverviewActivity"/>
        <activity android:name=".ui.owner.ManageOrdersActivity"/>
        <activity android:name=".ui.owner.ManageUsersActivity"/>
        <activity android:name=".ui.owner.ViewReportsActivity"/>

    </application>

</manifest>